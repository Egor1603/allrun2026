# 🏃 Беговые события России 2026

Сайт-календарь беговых событий по всей России с автоматическим обновлением.

🔗 **Сайт:** `https://ВАШ-НИК.github.io/ИМЯ-РЕПОЗИТОРИЯ/`

---

## Структура проекта

```
├── index.html                        # Сайт (фильтры по городу, месяцу, типу)
├── events.json                       # База событий (обновляется автоматически)
├── scraper.py                        # Скрипт парсинга официальных сайтов
├── .github/
│   └── workflows/
│       └── update.yml                # GitHub Actions: запуск 2 раза в сутки
└── README.md
```

---

## Шаг 1 — Загрузить файлы на GitHub

### Создать репозиторий
1. Зайдите на [github.com](https://github.com) → кнопка **+** → **New repository**
2. Имя: например `running-russia`
3. Видимость: **Public** (обязательно для GitHub Pages)
4. **Не** ставить галочку «Add README» — файлы загрузим вручную
5. Нажать **Create repository**

### Загрузить файлы
Нажать **Add file → Upload files** и загрузить:
- `index.html`
- `events.json`
- `scraper.py`

### Создать workflow вручную
Нажать **Add file → Create new file**:
- В поле имени написать: `.github/workflows/update.yml`
- Вставить содержимое файла `update.yml`
- Нажать **Commit changes**

---

## Шаг 2 — Включить GitHub Pages

1. Открыть **Settings** (вкладка в репозитории)
2. В левом меню → **Pages**
3. Source: **Deploy from a branch**
4. Branch: **main** → папка **/ (root)**
5. Нажать **Save**
6. Подождать 2–3 минуты — ссылка появится вверху раздела Pages

---

## Шаг 3 — Разрешить Actions записывать файлы

1. **Settings** → **Actions** → **General**
2. Прокрутить до «Workflow permissions»
3. Выбрать **Read and write permissions**
4. Нажать **Save**

---

## Шаг 4 — Добавить VK токен (опционально)

Без токена скрипт работает, но не парсит ВКонтакте.

1. Зайти на [vk.com/dev](https://vk.com/dev) → **Мои приложения** → **Создать**
2. Тип приложения: **Standalone**
3. После создания: **Настройки** → скопировать **Сервисный ключ доступа**
4. В GitHub: **Settings** → **Secrets and variables** → **Actions** → **New repository secret**
   - Name: `VK_TOKEN`
   - Value: ваш токен
5. Нажать **Add secret**

---

## Шаг 5 — Проверить что всё работает

1. Открыть вкладку **Actions** в репозитории
2. Нажать на **Update Events** → **Run workflow** → **Run workflow**
3. Дождаться зелёной галочки (1–2 минуты)
4. Открыть сайт — данные обновлены

После этого скрипт будет запускаться автоматически каждый день в **09:00** и **18:00 по МСК**.

---

## Как добавить событие вручную

Открыть `events.json` в репозитории → нажать карандаш (Edit) → добавить объект в массив `events`:

```json
{
  "id": 99,
  "date": "2026-09-20",
  "name": "Название забега",
  "city": "Город",
  "region": "Регион",
  "distances": "5 км, 10 км, 21,1 км",
  "type": "road",
  "url": "https://сайт-события.ru"
}
```

Поле `type`: `road` — шоссе, `trail` — трейл, `night` — ночной.

---

## Источники парсинга

| Сайт | Что парсится |
|---|---|
| krasmarafon.ru | Красноярск |
| pushkin-run.ru | Санкт-Петербург |
| heroleague.ru | Санкт-Петербург |
| ea-m.org | Екатеринбург |
| runsim.ru | Омск |
| tomskmarathon.ru | Томск |
| kazan.run | Казань |
| sib-events.ru | Алтай, Сибирь |
| wnmarathon.runc.run | СПб, Белые ночи |
| moscowhalf.runc.run | Москва |
| springrun.ru | Новосибирск |
| sportsauce.ru | Новосибирск |
| timerman.org | Казань, Татарстан |
| events.topliga.ru | Юг России |
| alpmarathon.ru | Иркутск |
| myrace.info | Трейлы, вся Россия |
| rtra.ru | Трейлы, вся Россия |
| skyrunning.ru | Горные старты |
| vk.com (API) | runningrussia, krasmarafon, begaem |


# Беговой календарь России 2026

**Сайт:** [allrunrus.ru](https://allrunrus.ru)  
**Репозиторий:** [github.com/Egor1603/allrun2026](https://github.com/Egor1603/allrun2026)

Автоматический агрегатор беговых событий России. Данные берутся с официальных сайтов организаторов и обновляются дважды в сутки через GitHub Actions.

---

## Структура файлов

```
allrun2026/
├── index.html          ← главная страница
├── events.json         ← база событий (главный файл данных)
├── scraper.py          ← парсер + генератор страниц городов
├── .github/workflows/
│   └── update.yml      ← автозапуск (06:00 и 15:00 UTC)
├── hero-map.png        ← фон шапки
├── favicon.svg
├── og-image.svg
├── sitemap.xml         ← генерируется автоматически
├── robots.txt
│
├── moskva/             ┐
│   └── index.html      │  Генерируются автоматически
├── ekaterinburg/       │  НЕ редактировать вручную
│   └── index.html      │
└── trail/ ...          ┘
```

---

## Как добавить событие вручную

1. Открой `events.json` на GitHub → карандаш (Edit)
2. Найди место по дате (файл отсортирован хронологически)
3. Добавь запись:

```json
{
  "id": 999,
  "date": "2026-08-15",
  "name": "Название забега",
  "city": "Екатеринбург",
  "region": "Свердловская область",
  "distances": "5 км, 10 км, 21,1 км",
  "type": "trail",
  "url": "https://сайт-организатора.ru"
}
```

4. Нажми **Commit changes** → сайт обновится через 2–3 минуты

### Поля события

| Поле | Обязательно | Описание |
|---|---|---|
| `id` | ✓ | Уникальный номер (максимальный + 1) |
| `date` | ✓ | Формат `YYYY-MM-DD` |
| `name` | ✓ | Название события |
| `city` | ✓ | Город проведения |
| `region` | ✓ | Область/регион — нужен для поиска |
| `distances` | — | Дистанции: `5 км, 10 км, 21,1 км` |
| `type` | ✓ | `road`, `trail` или `night` |
| `url` | ✓ | Сайт организатора (не агрегатор) |

---

## Как добавить новый источник в парсер

1. Открой `scraper.py`
2. Добавь функцию перед строкой `# ─── MERGE & SAVE`:

```python
def scrape_mysite():
    """mysite.ru — описание"""
    print("  mysite.ru")
    html = fetch("https://mysite.ru/events/")
    if not html:
        return []
    events = []
    # ... парсинг ...
    return events
```

3. Добавь в список `SOURCES` (vk всегда последним):

```python
("mysite.ru", scrape_mysite),
```

4. Commit → новые события подтянутся при следующем запуске

---

## Страницы городов и регионов

Генерируются **автоматически** при каждом запуске парсера.  
**Не редактируй папки городов вручную** — перезапишутся.

### Какие страницы создаются

| URL | Описание |
|---|---|
| `allrunrus.ru/moskva/` | Забеги Москвы |
| `allrunrus.ru/ekaterinburg/` | Забеги Екатеринбурга |
| `allrunrus.ru/ural/` | Все забеги Урала |
| `allrunrus.ru/kavkaz/` | Все забеги Кавказа |
| `allrunrus.ru/trail/` | Все трейлы России |
| `allrunrus.ru/road/` | Все шоссейные забеги |
| `allrunrus.ru/night/` | Все ночные забеги |

Страница создаётся для города если в нём **3+ предстоящих событий**.

### Изменить дизайн всех страниц сразу

Редактируй только `index.html` → при следующем запуске парсер применит дизайн ко всем городским страницам автоматически.

---

## Автоматическое обновление

**Расписание:** 06:00 UTC (09:00 МСК) и 15:00 UTC (18:00 МСК)

**Что делает парсер при каждом запуске:**
1. Парсит 48 источников
2. Добавляет новые события без дублей
3. Сохраняет прошедшие события
4. Генерирует страницы городов
5. Обновляет sitemap.xml
6. Делает commit → GitHub Pages публикует за ~2 мин

**Запустить вручную:**  
GitHub → Actions → Update Events → Run workflow

---

## Изменить дизайн сайта

Все изменения — в `index.html`:
- `<style>` — цвета, размеры, отступы
- `.hero` — шапка с картой
- `.filters-bar` — панель фильтров
- `<script>` — логика фильтрации и поиска

После commit при следующем запуске парсер применит дизайн ко всем страницам.

---

## SEO — зарегистрировать сайт

**Яндекс Вебмастер:** [webmaster.yandex.ru](https://webmaster.yandex.ru)
- Добавить сайт → HTML-файл → загрузить в репозиторий
- Индексирование → Sitemap → `https://allrunrus.ru/sitemap.xml`

**Google Search Console:** [search.google.com/search-console](https://search.google.com/search-console)
- Добавить ресурс → HTML-файл → загрузить в репозиторий
- Файлы Sitemap → `sitemap.xml`

---

## FAQ

**Добавил событие — когда появится?**  
Через 2–3 минуты после commit. Или при следующем плановом запуске.

**Как удалить событие?**  
Удали запись из `events.json` и сделай commit.

**Прошедшие события удаляются?**  
Нет — они в вкладке «Прошедшие» на сайте.

**Страница города не создалась?**  
Нужно минимум 3 предстоящих события в городе.

**Как добавить новый макрорегион?**  
В `scraper.py` найди словарь `MACRO_REGIONS` в функции `generate_city_pages`.

---

## Технический стек

- **Frontend:** HTML + CSS + Vanilla JS
- **Хостинг:** GitHub Pages
- **CI/CD:** GitHub Actions
- **Парсер:** Python 3 (urllib, re, json)
- **База данных:** events.json
- **Домен:** allrunrus.ru (DNS через Beget)
